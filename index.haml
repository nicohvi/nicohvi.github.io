---
title: I have no idea what I'm doing
layout: default
---

%h1 I have no idea what I'm doing

%ul
  {{% for post in site.posts %}
  %li
    .post-container
      %span.date
        %i.fa.fa-clock-o
        {{ post.date | date: "%-d %B %Y" }}
      -
      %a.post-link{ href: "{{ post.url}}" }
        {{ post.title }}
    .post-tags
      {% for tag in post.tags %}
        %span.tag {{ tag }}
      {% endfor }
  {{% endfor }}

