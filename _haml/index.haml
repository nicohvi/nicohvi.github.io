\---
layout: default
\---

%section.welcome
  .content
    %img.main-image(src="/public/images/pikachu.svg")
    %h1 Sup.

    %p 
      My name is Nicolay, and I like programming, drawing and
      writing. Preferably all at once. If you feel like reading
      some <span class="tag">#knowledgebombs</span>, feel free to scroll on down. Or, maybe
      you feel like <a href="#" class="shuffle">checking out some (awful) drawings</a>?

\{% for post in site.posts %}
%section{class: "post {{ post.color }}", data: { color: "{{ post.color}}" } }
  .content
    %img(src="/public/images/posts/{{ post.image }}")
    %section
      %h1
        %a(href="{{ post.url }}") {{ post.title }}
      \{% for tag in post.tags %}
      %span.tag {{ tag }}
      \{% endfor %}

    %p {{ post.teaser }}

\{% endfor %}

.notice.fade

